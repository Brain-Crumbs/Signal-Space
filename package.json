{
  "name": "signal-space",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=24.19.0 <25",
    "npm": ">=11 <12"
  },
  "packageManager": "npm@11.9.0",
  "scripts": {
    "dev": "npm run dev -w @signal-space/web",
    "cli": "npm run cli -w @signal-space/cli --",
    "research": "PYTHONPATH=python python3 -m signal_space",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "node --import tsx --test test/*.test.js test/*.test.ts",
    "test:research": "PYTHONPATH=python python3 -m unittest discover -s python/tests -v",
    "contracts:check": "python3 scripts/generate-research-contracts.py --check",
    "test:browser": "playwright test",
    "build": "npm run build -w @signal-space/cli && npm run build -w @signal-space/web",
    "research:manifest": "node scripts/research-archive.mjs --write",
    "verify:research": "node scripts/research-archive.mjs",
    "check": "npm run format:check && npm run lint && npm run typecheck && npm run contracts:check && npm run verify:research && npm test && npm run test:research && npm run build"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "tsx": "4.23.13",
    "eslint": "10.10.0",
    "@eslint/js": "10.0.1",
    "typescript-eslint": "8.70.0",
    "prettier": "3.9.6",
    "@playwright/test": "1.63.0",
    "@types/node": "24.10.1"
  }
}
