# Signal Space

Interactive and reproducible tooling for the Paper I directional signal-clock model.

The initial contract lives in [`docs/model-contract.md`](docs/model-contract.md), with paper coverage in [`docs/paper-i-traceability.md`](docs/paper-i-traceability.md). Run contract checks with Node 20+:

```sh
npm test
```
