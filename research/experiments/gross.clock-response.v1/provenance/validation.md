Both canonical runs and reader bundles verify. All seven PNG figures and all 18 setup/results PDF pages were rendered and visually reviewed. npm ci and npm run check passed, including format/lint/typecheck, contract generation, archive and bundle checks, Node/Python tests and both builds. CLI pair smoke and Markdown math audit passed. Browser OS dependency installation failed due sandbox setgroups/setegid permissions; browser-only download returned invalid/truncated archives. Browser integration was attempted and could not launch the missing Chromium executable. See saved logs. No hosted Actions run was dispatched. First full check stopped at the expected stale research catalog; regenerated the catalog and the full check then passed.

Publication: terminal git push lacked credentials after destination ownership was verified through the GitHub connector. Publishing uses GitHub git-data APIs. The exact local solver revisions cited by canonical provenance are preserved as solver-source tar archives; API-created commits have their own GitHub metadata and IDs.
