# Test 2 publication provenance

The initial clean-checkout pipeline ran solver revision `183b95feeae1cc1d4bd79ad3b3cd506f4952fba0` and generated report-0001. Its logs and status retain that original stage history. A presentation-only revision `0ba0865a4d2f6f900ab43986a6f6e77af2c329b0` generated report-0002 from the same analysis and raw bytes, correcting a clipped annotation and clarifying zero/null presentation. Both reports remain in the canonical package. The final reader selects report-0002 and includes the reviewed interpretation.

The initial source archive is not duplicated here; its SHA-256 is recorded in initial-pipeline-execution.json. Direct Git push credentials were unavailable, so the connected GitHub app publishes the identical final tree under its own commit identity. The original clean execution and renderer commits are preserved in `source-history.bundle` (base prerequisite `ea0ad384d4f55e6ea117ab20c2759e2bf502d1f3`, already on main). Recover them with `git fetch path/to/source-history.bundle evidence/gross-test-02-execution`; the fetched commit `0ba0865` contains solver ancestor `183b95f`. Then regenerate the exact original source archive with `git archive --format=tar.gz 183b95f`. The recorded solver commit is therefore a recoverable execution identity, not the GitHub publication commit. No solver retry or changed scientific threshold occurred. The committed canonical package and reader export were reverified after report-0002; their hashes are paired.

Local npm run check passed, including 172 JavaScript and 64 Python tests, plan/bundle contracts and production builds. The built CLI sample passed. Browser dependency installation failed because apt could not change process groups in this environment; browser-only download returned invalid/truncated archives. Browser tests therefore could not run. This is an environmental validation gap, not evidence that browser integration passes. CI still does not run npm test suites.
