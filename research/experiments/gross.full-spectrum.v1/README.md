# Signal Space / GROSS Test 4: complete reciprocal spectrum

The bounded experiment is technically complete. Eight implementation/control checks pass. The strong all-sector vacuum common-cone hypothesis fails; opposing-wave background stability remains unresolved. Generic noncollinear active-memory backgrounds and autonomous clocks remain open.

Start with [the reviewed reader export](export-run-5a55cb5041050589/README.md), [results PDF](export-run-5a55cb5041050589/Experiment_Results.pdf), or [interpretation and next calculation](export-run-5a55cb5041050589/Experiment_Analysis_and_Next.md).

- Selected run: `run-5a55cb5041050589`.
- Analysis: `analysis-0001-bceef551`; report: `report-0001`.
- Model: `signal-space.ss-ops-1.six-gate.v1`, using the unchanged local SS OPS 1 Hamiltonian with an explicit two-port/six-memory incidence.
- Three exact backgrounds, all 20 physical tangent dimensions, five interpreted figures, exact plot data, setup/results PDFs and source code.
- Runtime: 22.774 seconds for the complete local recipe. Run it again through Actions > Run experiment > gross-test-04 after merge. No output is automatically committed.

The [canonical run](run-5a55cb5041050589/manifest.json) preserves immutable evidence. The provenance directory includes prior pipeline failures with their completed raw runs, validation logs, and an exact source-history Git bundle. Prior attempts are not relabeled as the selected run. Direct Git push credentials were unavailable; publication uses the connected GitHub app with identical tree content. The original solver, analysis and renderer commits remain recoverable from the bundle against main's history. Local source-archive hashes in execution records can be reproduced with `git archive` at those commits.

Full local checks passed (172 JavaScript and 73 Python tests, agent contracts, lint/types, archive/exports and builds). Browser validation is blocked by unavailable Chromium and denied OS dependency installation; the logs record this. GitHub-hosted experiment dispatch/upload remains untested. No merge or deployment was performed.
