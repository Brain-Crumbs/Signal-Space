# Validation record

- Node 24.19.0 and npm 11.9.0; npm ci completed.
- Full local npm run check passed: formatting, ESLint, strict types, generated contracts, research archive, reader exports and agent contract tests, all 172 JavaScript tests, all 68 Python tests and both production builds.
- After this full check, the configuration schema was tightened to reject boolean numeric entries and a focused recipe assessment test was added. The four router tests and seven recipe tests passed again; the locked configuration still validates and estimates successfully. No equations, configuration values, thresholds or saved measurements changed.
- Built CLI pair sample passed; saved output is cli-sample3.json.
- Both prior Test 1 and Test 2 canonical packages reverified. Test 3 canonical verification: 1 attempt, 1 analysis, 1 report, 63 artifacts, valid. Reader bundle validated. Plan hash and exact runtime configuration validated before execution and after schema hardening.
- All six figures and 13 reader PDF pages visually inspected. The reviewed export preserves the same figure/report bytes and raw run; only the mentor assessment differs from the automated recipe export.
- Browser integration is blocked in this environment. Playwright dependency installation failed because apt cannot change groups/user IDs; npm run test:browser could not launch the missing Chromium executable. Logs are included. This is not a browser-test pass.
- GitHub-hosted manual dispatch/upload has not been executed. The exact scientific recipe completed locally in 19.618 seconds.
- Direct git push has no interactive credentials; publication uses the authenticated GitHub app. source-history.bundle preserves the original solver commit. The final remote Git tree is compared to the local tree before PR creation.

Initial integration preflight rejected an unset config fingerprint before any execution. It was filled and the plan locked before the sole scientific run. An intermediate archive check detected a stale archive manifest after adding publication files; the manifest was regenerated and the full check above passed. Neither diagnostic changed a scientific criterion or raw evidence.
