# E00 synthetic fixture analysis and next calculation

The saved analysis compares the 24-step synthetic trace with an independent recurrence. The classification and exact residual belong to the selected run's `results/tables/analysis.json` and `results/tables/checks.json`; this handoff does not infer physical dynamics.

The figure illustrates an integrity check and cannot support a spacetime claim. Next, select a registered physical model and preregister competing propagation signatures, independent mesh and boundary controls, and a visual that compares predicted and observed arrival events.
